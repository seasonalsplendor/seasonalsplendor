import ContactClient from './ContactClient'

export const metadata = {
  title: 'Book a Consultation | Seasonal Splendors NYC',
  description: 'Request a seasonal decorating consultation for your Upper West Side brownstone. Seasonal Splendors serves the UWS exclusively — design, installation, and removal included.',
}

export default function ContactPage() {
  return <ContactClient />
}
